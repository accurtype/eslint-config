{
	"$schema": "https://json.schemastore.org/package",
	"private": true,
	"name": "accurtype-eslint-config",
	"license": "MIT",
	"author": "accurtype",
	"repository": {
		"url": "git+https://github.com/accurtype/eslint-config",
		"type": "git"
	},
	"type": "module",
	"scripts": {
		"prepare": "husky",
		"cz": "pnpm -s lint && cz",

		"fmt": "prettier -w . && eslint . --fix",
		"lint": "prettier -c . && eslint .",

		"test": "pnpm run -r test",
		"build": "pnpm run -r build",
		"clear": "pnpm run -r clear",
		"ci": "pnpm build && pnpm test && pnpm clear && pnpm lint"
	},
	"devDependencies": {
		"husky": "^9.*.*",

		"@commitlint/cli": "^20.*.*",
		"commitizen": "^4.*.*",
		"cz-customizable": "^7.*.*",

		"eslint": "^9.*.*",
		"prettier": "latest",

		"rimraf": "latest",
		"make-dir-cli": "latest",
		"tsx": "latest",
		"typescript": "latest"
	},
	"config": {
		"cz-customizable": {
			"config": "./config/cz-config.cjs"
		},
		"commitizen": {
			"path": "./node_modules/cz-customizable"
		}
	},
	"prettier": {
		"printWidth": 200,
		"tabWidth": 4,
		"useTabs": true,
		"semi": true,
		"singleQuote": true,
		"jsxSingleQuote": false,
		"trailingComma": "all"
	}
}
